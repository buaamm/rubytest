<h1>New User</h1>
<%= form_for :user, url: users_path, :html => { :multipart => true } do |f| %>
  <%= render 'form', :f => f %>
  <p>
    <%= f.submit 'Click To Create' %>
  </p>
<% end %>


<%= link_to 'Back to Users', users_path %>

