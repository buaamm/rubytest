<h1>Listing articles</h1>

<%= link_to 'New article', new_article_path %>
<table>
    <tr>
        <th>ID</th>
        <th>Title</th>
        <th>Text</th>
        <th>CreateDate</th>
        <th colspan="3"></th>
    </tr>

    <% @articles.each do |article| %>
        <tr>
            <td><%= article.id %></td>
            <td><%= article.title %></td>
            <td><%= article.text %></td>
            <td><%= article.created_at %></td>
            <td><%= link_to 'Show', article_path(article) %></td>
            <td><%= link_to 'Edit', edit_article_path(article) %></td>
            <td><%= link_to 'Destroy', article_path(article), method: :delete,
                data: { confirm: 'Are you sure?' } %>
            </td>
        </tr>
    <% end %>
</table>

  <%-# <%= render 'youku', :code => "XOTEwMDI4MzI4", :title => "Another embeded Video" -%>

<% content_for :player do %>
  <link rel="stylesheet" href="/build/css/base.min.css?1" />
  <script src="/build/js/CommentCoreLibrary.min.js"></script>
  <script src="/build/js/ABPlayer.min.js"></script>

    <script type="text/javascript">
    window.onload=function() {
      var inst = ABP.create( document.getElementById('load-player'), {
        'src': document.getElementById('video-1'),
        'width': 400,
        'height': 300
      });
      CommentLoader('xml/comment-otsukimi.xml', inst.cmManager);
    }
    </script>
<% end %>

    <p>Danmaku HTML5</p>
    <video id="video-1" autobuffer="true" data-setup="{}" width="400" height="250">
      <source src="/media/otsukimi_recital.mp4" type="video/mp4">
      <p>Your browser does not support html5 video!</p>
    </video>
    <div id="load-player"></div>


<p><%= link_to 'New Article', new_article_path, class: "button blue" %></p>

<p>-</p>
<p><%= link_to 'Home Page', welcome_index_path, class: "button orange" %> | 
<%= link_to 'Articles', articles_path, class: "button gray" %>
</p>
<p>-</p>
<p><%= link_to 'Albums', albums_path, class: "button orange" %> | 
<%= link_to 'Upload Videos', videos_path, class: "button orange" %>
</p>

